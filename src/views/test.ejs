<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Album Card</title>
  <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .album-card {
  position: relative;
  overflow: hidden;
  border: 1px solid #ddd;
  border-radius: 5px;
  transition: transform 0.3s, box-shadow 0.3s;
}

.album-card:hover {
  transform: scale(1.05);
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

.album-card img {
  width: 100%;
  height: auto;
}

.card-overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  background: rgba(0, 0, 0, 0.6);
  color: #fff;
  opacity: 0;
  transition: opacity 0.3s;
  padding: 10px;
}

.album-card:hover .card-overlay {
  opacity: 1;
}

.card-header {
  text-align: center;
}

.card-title {
  margin: 0;
  font-size: 1.25rem;
}

.card-date {
  margin: 0;
  font-size: 0.9rem;
}

.card-description {
  display: flex;
  align-items: flex-end;
  justify-content: center;
  opacity: 0;
  transition: opacity 0.3s, transform 0.3s;
}

.album-card:hover .card-description {
  opacity: 1;
  transform: translateY(-20px);
}

.album-card:hover .card-header {
  transform: translateY(-40px);
}

  </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row">
          <div class="col-md-4">
            <div class="album-card">
              <div class="card-overlay">
                <div class="card-header">
                  <h5 class="card-title">Album Title</h5>
                  <p class="card-date">Created on: 2024-07-11</p>
                </div>
                <div class="card-description">
                  <p>This is a description of the album. It gives a brief overview of the content within.</p>
                </div>
              </div>
              <img src="https://via.placeholder.com/300x200" class="card-img-top" alt="Album Image">
            </div>
          </div>
        </div>
      </div>
</body>
</html>
